# 更新日志

本文件记录“小黑盒bot”的版本变化。使用方法、配置与部署说明请查看 [README.md](./README.md)。

## v0.6.42 - 2026-08-13

### 新增

- 评论洞察新增无需主题或关键词的“自动探索”模式。管理员只需让爱丽丝分析某个帖子或时间范围，模型会自动归纳主要话题、情绪、夸奖、吐槽、提问、建议、玩梗、争议与值得注意的信号，并返回代表评论。
- 自动探索结果支持 SQLite 分类缓存和最终主题合并；重复分析相同模型下未变化的评论时复用已有分类。评论超过单次模型上限时跨整个时间范围均匀抽样，并在报告中显示样本覆盖率。

### 优化

- `xhh_comment_insights` 的 `topic` 改为可选。工具描述明确要求在“看看大家都在说什么”这类请求中直接启动自动探索，不再反问用户提供关键词；原有按主题的精确统计保持兼容。
- WebUI“评论洞察”的分析主题、关键词和表情均可留空，结果页增加自动主题、情绪、常见问题、建议、争议和整体摘要展示。配置页与 README 同步说明两种分析模式。
- 修复 WebUI 已有洞察结果时“暂无分析结果”占位层仍占据页面空间的问题。

## v0.6.41 - 2026-08-12

### 修复

- 修复远程图片下载只读取首个网络分块的问题。头像、帖子图片、评论图片和远程图片上传现在会在大小上限内持续读取到响应结束，避免完整尺寸的图片实际只有顶部少量像素、其余区域透明或空白。
- 远程图片分块读取过程中一旦超过配置的下载上限会立即停止并返回明确错误，避免为完整读取放宽内存保护。

## v0.6.40 - 2026-08-12

### 修复

- 修复 WebUI 头像图片加载后首字占位仍可能显示在图片上方的问题。头像现在会先完成浏览器解码和尺寸检查，确认有效后再隐藏占位并显示图片。
- 账号头像兼容公开资料结果根层字段和扫码登录结果；公开资料、扫码结果与登录 Cookie 会按顺序作为候选来源。某个候选下载失败或是异常小的占位图时，会自动继续尝试下一项。
- 拒绝将边长小于 16 像素的图片缓存为账号头像，避免 1×1 占位图表现为“只加载了一点点”。

### 优化

- 运行状态顶部改为紧凑概览带：账号头像与公开资料、待处理、评论归档、私信归档合并展示，移除重复的大账号统计卡片，让账号、队列和自动处理明细更早进入首屏。
- 手机端保持三项核心统计并排显示，头像和账号资料独立一行，避免横向溢出。

## v0.6.39 - 2026-08-12

### 修复

- 修复当前账号资料未直接返回头像字段时，WebUI 不创建头像槽位、也不会继续请求头像接口的问题。登录后现在始终显示头像位置，并可从扫码登录保存的头像信息回退读取。

### 优化

- 当前账号头像由插件服务器受限读取并转换为图片数据返回，避免浏览器直接访问小黑盒图片地址；头像缓存一小时，点击“刷新”可强制更新。
- 头像下载会携带小黑盒页面来源信息，并且只向小黑盒及其图片 CDN 发送账号 Cookie；第三方图片域名不会收到登录凭据。头像暂时读取失败时保留昵称首字占位。

## v0.6.38 - 2026-08-12

### 新增

- 运行状态新增当前账号公开资料摘要，可显示实际取得的头像、等级、关注数、粉丝数、帖子数、评论数、个性签名和 IP 属地。
- 页面右上角“刷新”、`/小黑盒状态` 和 `xhh_status` 工具会强制重新读取当前账号资料；后台轮询按短缓存自动更新，账号改名后无需重新登录。

### 优化

- 自动回复、私信、自动巡帖通知以及 WebUI 中原有的“Bot 回复”“Bot 评论”等标签改为当前登录小黑盒账号昵称，并保留原有消息、帖子、评论和用户 ID。
- 消息数据库改为评论列表优先布局：统计与评论/私信切换压缩到一行，筛选区收窄，详细处理状态移动到列表后的折叠区。
- 终端风格减少 `./`、`>`、`::` 和方括号等装饰，仅保留少量品牌提示符、状态灯与动态网络背景。
- 加强 `xhh_comment_insights` 的自然语言描述，使模型更容易处理“多少人在夸我”“有多少人吐槽某话题”等评论分析请求；工具仍仅限管理员读取。

## v0.6.37 - 2026-08-12

### 新增

- 新增“评论洞察”：可对 SQLite 已归档评论统计字面关键词、小黑盒标准表情和 LLM 语义近义表达，输出重叠去重后的并集、占比、覆盖率与代表样例。
- 新增语义分类 SQLite 缓存和后台分批任务，支持缓存复用、进度查询与管理员取消；工具 `xhh_comment_insights` 支持 `run`、`status`、`cancel` 三种模式。
- 新增“启用评论语义洞察”“评论洞察模型”“语义分析单批评论数”“单次语义分析评论上限”“洞察代表样例上限”配置项。

### 优化

- 插件 WebUI 重做为终端黑客风界面，加入动态网络背景、扫描线和状态灯，同时保留运行控制、扫码登录、消息数据库、分页查询和详情查看能力。
- 新增 WebUI“评论洞察”工作区，支持筛选条件、后台进度、缓存与模型请求统计、代表评论和任务取消；适配桌面与移动端，并支持系统减少动态效果设置。
- 代表样例固定按归档记录顺序输出；关闭“WebUI 显示消息正文”后不返回代表评论原文。
- README 补充评论洞察统计口径、缓存和配置说明，并统一使用配置页中文名称。

## v0.6.36 - 2026-08-11

### 优化

- HTTP 图片默认改为先下载原始字节后上传到小黑盒 COS，避免 URL 转存把缩略图再次作为原图；小黑盒 CDN URL 中的 `imageMogr2`、`imageView2`、水印等图片处理参数会自动剥离。原始下载或上传失败时保留 URL 转存回退，并写入后台警告。
- 新增 `media.preserve_remote_image_bytes`（中文说明：`HTTP 图片优先原字节上传`），可按需关闭原字节上传策略。
- 自动回复中的 AstrBot `Image` 消息段现在优先使用本地原文件路径，而不是同一消息段中的预览 URL。
- 表情识别覆盖 `cube`、`heygirl`、`bigemoji` 与 `grandemoji` 共 156 个标准标识符，并兼容每个标准名称的常见中文、英文与历史写法；实时账号列表仍优先。
- 新增 [小黑盒表情对照表](./emoji_reference.md)，记录完整标准标识符、兼容规则与维护方式。

## v0.6.35 - 2026-08-08

### 修复

- 兼容模型生成的中文狗头表情标记：`[cube_狗头]` 现在会规范为小黑盒实际支持的 `[cube_doge]`，不会再降级为普通文字 `[狗头]`。

## v0.6.34 - 2026-08-07

### 修复

- 修复私信启用外部搜索或其他 LLM 工具时，AstrBot 的零宽占位输出可能被提前发送到小黑盒，导致真正的最终回答被后续重复发送保护忽略的问题。
- 现在零宽占位消息只会被记录为调试日志，不会发送、不会写入“回复成功”，也不会占用该条私信的发送机会；工具调用结束后的实际回答会正常发送到原私信会话。

## v0.6.33 - 2026-08-06

### 修复

- 修复自己帖子下的评论带图片时，机器人把关联帖子图片误当成当前评论图片的问题。
- 评论回复现在会按评论 ID读取帖子详情中的评论树；楼中楼评论会按根评论 ID补查，并将当前评论、被回复评论和原帖图片分开传入视觉模型。
- 兼容旧版本已进入队列的评论记录：如果其中保存的是帖子封面，处理时会移除该错误图片，避免继续污染当前评论的视觉上下文。

## v0.6.32 - 2026-08-04

### 新增

- 新增“单个自己帖子自动回复上限”配置，默认 50，`0` 表示不限。插件会在入队、处理旧队列和真正发送前校验额度，确认发送与发送结果不确定都会计入，避免热门帖子无限占用自动回复队列。
- 单帖回复计数写入持久化运行状态，并从现有评论 SQLite 归档恢复历史数据；降低上限后，超额积压会自动标记为跳过。
- 新增 `/小黑盒清空队列 确认` 和 `/小黑盒清空队列 帖子ID 确认`。命令只取消待处理和已提交的评论，保留已经开始发送的项目以及消息游标、失败记录、归档、统计和登录信息。
- WebUI“运行状态”新增停止/继续后台任务、取消全部队列和按帖子取消队列的应急控制，并显示当前单帖回复上限。

## v0.6.31 - 2026-08-04

### 修复

- 修复标准事件模式下单轮最大回复数高于并发上限时只提交前几条评论的问题。插件现在会在并发槽位释放后继续处理本轮批次，同时保留 `event_bridge.max_in_flight` 的压力保护；无法及时释放的剩余评论继续留在队列中，并写入明确的后台日志。
- 修复自己帖子下的评论图片没有传入 AstrBot 标准消息链的问题。当前评论图片、被回复评论图片和原帖图片现在都会按顺序交给视觉模型。
- 兼容小黑盒评论图片返回的 `img`、别名字段以及分号/逗号拼接的图片 URL。

## v0.6.30 - 2026-08-04

### 优化

- WebUI 消息数据库分页区新增当前页与总页数，评论和私信列表都会显示“第 n / m 页”；空结果显示“0 条记录，共 0 页”。

## v0.6.29 - 2026-08-03

### 修复

- 修复关闭“每次写操作都要求确认”后，模型仍可能受插件启动时注册的旧工具参数影响而继续索取确认的问题。
- 写工具参数不再包含可由模型填写的 `confirm` 字段；是否需要确认改为在每次执行时读取当前配置，并只校验用户当前原始消息中的确认词。开关保存后无需重载插件，开启时的确认保护不变。

## v0.6.28 - 2026-08-03

### 修复

- 修复纯文本发帖把真实换行转换成字面量 `<br>` 的问题；帖子正文现在直接发送真实换行，避免在小黑盒正文中显示 `<br>`。
- 兼容 LLM 工具参数中的字面量 `\\n`、`\\r\\n` 和 `\\r` 换行写法。

## v0.6.27 - 2026-08-02

### 修复

- 修复小黑盒发帖接口返回“请添加分区”但帖子已经落地时仍发送失败通知的问题：插件会回查当前账号最近帖子，按标题和正文确认后返回成功。
- 删除接口返回“帖子已经删除”时按幂等成功处理，避免重复删除或误报。

## v0.6.26 - 2026-08-02

### 修复

- 统一处理帖子正文、评论回复和私信中的常见 HTML 换行标记，将 `<br>`、`<br />`、`<p>` 和 `<div>` 转换为真正换行，避免标记直接显示或内容挤在同一行。

## v0.6.25 - 2026-08-02

### 新增

- 自动评论、评论回复、私信回复和自动巡帖会获得 AstrBot 服务器当前日期、时间、星期与时区元数据，帮助模型正确理解相对时间。
- 新增 `ai.allow_external_search`（中文说明：`允许回复时使用外部搜索`）。开启后，模型可以在回复前自主调用当前已启用的搜索、网页检索或正文提取工具核实可能过时或不确定的信息。
- 兼容 `astrbot_plugin_anysearch` 等搜索插件；搜索工具只读筛选，不会向小黑盒外部消息开放发帖、评论、私信、删除、点赞、关注或收藏工具。
- 搜索工具循环失败、不可用或没有合适事件上下文时自动回退普通模型生成，不影响原有回复流程。
- 修复 LLM 发帖正文中的 `<br>` 或 `<br />` 被当作普通文字显示的问题；普通 `body` 字段现在会将常见换行和段落标记转换为真正换行，同时保留其他普通文本。

## v0.6.24 - 2026-08-02

### 新增

- 新增 `notifications.notify_on_error` 可选错误通知。开启并填写 `notifications.umo` 后，后台轮询、自动回复、私信、自动巡帖、图片处理、标准事件和 LLM 工具运行错误会发送摘要。
- 错误通知包含错误类别、类型、信息和必要的相关 ID，并过滤 Cookie、签名和令牌等敏感字段；相同错误 60 秒内只通知一次，通知发送失败不会影响原处理流程。

## v0.6.23 - 2026-08-02

### 优化

- GIF 首帧转换失败时增加截断帧兼容处理，尽量从 CDN 不完整或特殊编码的 GIF 中提取可用首帧；仍无法解码时只跳过该图片，不影响文字回复。

## v0.6.22 - 2026-08-02

### 修复

- 修复普通社区请求仍使用旧版签名算法，导致小黑盒图片上传初始化接口返回“验证参数错误”的问题；普通接口现在与网页端统一使用规范化路径和网页签名。
- 移除普通社区请求中网页端不会发送的 `_notip=true` 参数，避免触发上传接口的参数校验。

## v0.6.21 - 2026-08-02

### 修复

- 修复本地图片上传初始化请求没有完全使用网页端 UTF-8 表单格式，导致 `/bbs/app/api/qcloud/cos/upload/info/v2` 返回“验证参数错误”的问题。
- 普通 POST 请求现在统一显式发送 `application/x-www-form-urlencoded;charset=UTF-8`，并使用与网页端一致的表单编码。

## v0.6.20 - 2026-08-02

### 修复

- 修复普通社区 API 使用 `app=web` 导致图文发帖可能返回“验证参数错误”的问题，改为使用网页端实际使用的 `app=heybox`；扫码登录接口仍使用 `app=web`。
- 小黑盒业务接口拒绝请求时，在错误信息中附带具体接口路径，便于从 AstrBot 后台日志区分图片上传和最终发帖失败。

## v0.6.19 - 2026-08-02

- 修复图文发帖图片块缺少 `width`、`height` 导致小黑盒返回“验证参数错误”的问题。
- 本地图片使用实际尺寸生成发帖内容块，远程图片也会带上网页端兼容所需的尺寸字段。

## v0.6.18 - 2026-08-02

- 修复普通图文帖使用文章帖请求参数导致小黑盒返回“验证参数错误”的问题：正文块改为 `text`，普通话题使用正确的 `link_tag=27`。
- 发帖时规范化小黑盒表情 token；空话题和空标签不再发送多余字段。
- 本地草稿与发帖工具的话题 ID 都兼容数字和字符串。

## v0.6.17 - 2026-08-02

### 修复

- `xhh_publish_post` 的 `topic_ids` 同时兼容模型传入的数字数组和字符串数组，避免正常的数字话题 ID 在 AstrBot 工具参数校验阶段被拒绝。
- 默认允许读取 AstrBot `data/temp` 中由消息链或图片工具生成的临时图片；仍然只允许该临时目录，不会放开整个 AstrBot 数据目录。

## v0.6.16 - 2026-08-02

### 修复

- 修复主动评论或回复生成 `[cube_吐血]` 时被原样发成普通文字的问题，统一规范为小黑盒实际使用的 `[cube_吐]`。
- 表情元数据接口为空或暂时不可用时，使用官方标准表情目录兜底，未知标记仍会降级为普通文本。

## v0.6.15 - 2026-08-02

### 修复

- 自动巡帖、评论、私信和标准事件的视觉输入遇到 GIF 时，会先转换为首帧 PNG 再交给 AstrBot，避免 Gemini 因不支持 `image/gif` 重试并导致该条评估失败。
- GIF 下载或转换失败时只跳过这张图片，仍会使用文字上下文继续生成回复，并在 AstrBot 后台日志记录原因。
- 统一校验主动评论、自动回复、私信和 LLM 写工具中的小黑盒表情标记；有效的 `[cube_表情名]` / `[heygirl_表情名]` 会原样保留，不支持的标记会降级为可读文本，避免把错误表情协议发出去。

## v0.6.14 - 2026-08-01

### 配置

- 移除自动巡帖“24 小时评论上限”的人为最大值，配置项现在只要求正整数，额度由用户自行决定。
- 保留发送结果不确定计入额度、单轮评论上限、作者冷却和帖子去重等独立保护机制。

## v0.6.13 - 2026-08-01

### 修复

- 修复自动巡帖“24 小时评论上限”运行时被错误限制为 20 条的问题；配置项现在支持 1-50 条，与实际读取逻辑一致。

## v0.6.12 - 2026-07-30

### 修复

- WebUI 登录二维码改为二维码矩阵并使用页面内 Canvas 绘制，不再依赖可能在 AstrBot iframe 中显示失败的 `data:` 图片。
- 刷新扫码登录页时会重新取得尚未过期的二维码；状态轮询期间也会持续显示正确的二维码过期时间。

## v0.6.11 - 2026-07-30

### 修复

- WebUI“清除登录”不再依赖 iframe 中可能被浏览器拦截的原生确认框，改为页面内确认对话框。
- 清除过程中会禁用确认按钮并显示处理状态；成功后立即刷新登录与运行状态，失败时显示接口错误。
- 配置页仍填写“手动 Cookie（可选）”时，清除成功提示会说明重载插件后仍会重新使用该 Cookie。

## v0.6.10 - 2026-07-30

### 修复

- 二维码创建与状态轮询改为只发送小黑盒登录接口需要的参数，不再混入普通社区接口的签名和客户端参数，并与私信请求使用相同的网页 User-Agent。
- 扫码成功后只保存 `user_pkey`、账号 ID、昵称、头像、等级和平台实际返回的 `x_xhh_tokenid` 等必要登录 Cookie；不再自行补造缺失的 `x_xhh_tokenid`，并支持从登录结果的 `heyboxid` 字段读取账号 ID。
- 登录阶段与日常请求使用不同的 Cookie 会话。扫码完成后会关闭登录会话并建立不接收额外 Cookie 的干净会话，私信发送前也会过滤旧版本凭据中的无关 Cookie，避免二维码页面状态污染私信请求。
- 重新扫码登录会清除插件内的私信临时暂停状态，可以立即验证新登录态。
- 私信被平台拒绝时增加脱敏诊断日志，记录参数来源、客户端版本、设备 ID 哈希、Cookie 名称、代理开关和 User-Agent，不记录 Cookie 值、设备 ID 或代理地址。

### 升级说明

- 从旧版本升级后，如果私信曾提示“您已被禁止发送消息行为”，请确认“手动 Cookie（可选）”为空，执行 `/小黑盒退出`，再执行 `/小黑盒登录` 重新扫码。旧登录凭据不会自动变成新的干净登录态；退出登录不会删除 SQLite 消息数据库。

## v0.6.9 - 2026-07-30

### 修复

- 私信发送改用独立的 `app=heybox` 网页请求参数、当前网页签名规则和显式 UTF-8 URL 编码表单，请求形状与已验证可用的小黑盒适配器私信链路保持一致；评论、发帖和其他接口不受影响。
- 私信响应仍会校验服务端消息 ID，网络中断仍按“发送结果不确定”处理，不会为了兼容请求而取消消息去重、额度或冷却保护。
- 小黑盒明确拒绝私信时，不再永久阻止本次插件进程的全部后续私信。触发消息会跳过，后续外发默认临时暂停 1800 秒并自动恢复。

### 配置

- 新增“私信被拒后暂停（秒）” (`direct_messages.restriction_pause_sec`)；设为 `0` 时只跳过当前失败消息，不暂停后续私信。
- 新增“私信真实 API URL（可选）” (`direct_messages.api_params_url`)；可复用浏览器真实请求中的客户端参数白名单，签名、目标用户和 Cookie 不会从 URL 导入。

## v0.6.8 - 2026-07-27

### 修复

- 评论标准事件在创建前原子认领队列记录，并在真正外发前按“帖子 ID + 评论 ID”再次认领。同一条评论即使同时从 `@` 和自己帖子评论入口出现、事件延迟或同一事件被重复调用 `send()`，也只允许一次小黑盒外发；被拦截的重复事件会写入日志而不会再次回帖。
- 入站评论图片会同时解析顶层与嵌套的 `comment_a` / `comment_b` 数据。标准事件消息链和兼容生成路径都会按“本评论图片、被回复评论图片、帖子图片”标记、去重并传给视觉模型。

### 配置

- 新增 `ai.max_context_images`，默认 `8`。它限制一次评论回复交给模型的图片总数；当前评论图片优先，其次是被回复评论和帖子图片。`0` 可完全关闭视觉输入。

## v0.6.7 - 2026-07-27

### 修复

- 将小黑盒返回的“禁止发送消息行为”等明确插件会话私信发送限制识别为不可重试的终止状态。触发的私信会写入 SQLite 为已跳过；本次插件运行会暂停后续自动私信发送，继续收信和归档，并仅向配置的通知会话发出一次告警。
- 已知的平台私信限制不再重新抛给 AstrBot Core，避免产生误导性的发送失败堆栈；WebUI 与 `/小黑盒状态` 会明确显示自动私信已被平台限制暂停及其原因。

## v0.6.6 - 2026-07-27

### 修复

- 修复标准 AstrBot 事件超时后仍可能由原事件迟到发送、与重试事件重复回复的问题。未开始向小黑盒外发的事件会被明确终止后再重试；已进入外发阶段的超时则保留为“结果不确定”，停止自动重试。状态页和归档会给出对应原因。

## v0.6.5 - 2026-07-26

### 修复

- 修复 AstrBot 标准事件回复可能把内部用户命名空间 `xhh:<id>` 直接发到小黑盒的问题。出站 `At` 组件会忽略内部小黑盒标识，模型纯文本中意外回显的该标识也会在评论、私信和兼容的旧回复路径中移除；评论仍通过楼中楼 API 正确指向原评论。

## v0.6.4 - 2026-07-26

### 新增

- 增加当前账号便捷读取工具：`xhh_get_notifications` 合并 `@` 与自己帖子下的评论/回复，`xhh_get_my_favorites` 读取已收藏帖子，`xhh_get_remote_drafts` 读取小黑盒服务端草稿；`xhh_status` 也会返回当前账号 ID 和昵称，便于复用 `xhh_get_user_activity` 查询自己的帖子、评论或动态。
- `xhh_publish_post` 支持有序 `content_blocks` 富文本内容块，包含纯文本、安全 HTML 与图片；本地 `xhh_save_draft` / `post_drafts.sqlite3` 同步保存该结构，并自动迁移已有草稿库。

### 安全与兼容

- 富文本 HTML 使用允许列表，仅保留基础排版、强调、列表、引用、代码和公开 HTTP(S) 链接；拒绝脚本、样式、事件属性、私网或危险链接、内嵌图片和未知标签。
- 保留 `body`、`image_urls`、旧版本地草稿和既有发帖流程的兼容性；入站帖子新增安全的有序内容块保留，同时继续提供纯文本正文和完整图片链。

## v0.6.3 - 2026-07-26

### 新增

- 增加插件本地 SQLite 草稿箱，支持通过 `xhh_get_drafts` 读取、`xhh_save_draft` 保存或更新、`xhh_delete_draft` 删除发帖草稿。
- 增加 `tools.enable_draft_tools` 独立开关，默认关闭；关闭时草稿工具不会注册。开启后，读取沿用私密读取权限，保存和删除仍同时受总写工具开关、管理员/允许列表、冷却、重复写入和逐次确认保护。

## v0.6.2 - 2026-07-26

### 修复

- 评论标准事件的群组 ID 改为纯数字帖子 ID，兼容 `cr4zythursday` 等会将群组 ID 转为整数的 AstrBot 插件；会话 ID 与小黑盒用户命名空间保持不变。
- 发送到小黑盒前拦截 AstrBot 生成的插件处理异常诊断，纯诊断不会公开发布，正常回复后附带的诊断只会移除异常部分，普通 `:(` 文本不受影响。
- 小黑盒评论和被回复内容中的 HTML 链接、换行标签与实体会转换为可读纯文本，不再把 `<a ...>` 标签带入人设上下文和回复通知。

## v0.6.1 - 2026-07-26

### 修复

- WebUI 在 AstrBot 自动注入 Bridge SDK 晚于页面内联脚本时，不再缓存未定义的 Bridge 对象。
- 页面会在应用脚本前显式加载官方 Bridge SDK，并在初始化与 API 调用时重新获取和校验 Bridge，避免运行状态页停在骨架屏并提示 `Cannot read properties of undefined (reading 'ready')`。
- 增加 Bridge 加载顺序回归测试，并在 SDK 加载失败时显示可操作的版本与刷新提示。

## v0.6.0 - 2026-07-26

### 新增

- 评论和私信可作为 AstrBot 标准消息事件处理，沿用人设、会话历史、`astrbot_plugin_worldbook` 和其他 LLM 请求钩子。
- 增加好友/陌生人私信轮询与自动回复，包含首次基线、静默时段、用户冷却、24 小时额度、重试和发送结果不确定保护。
- 收到的评论、被回复内容、帖子和私信图片会组成完整消息链；回复与写工具支持网络、本地和 Base64 图片，小黑盒私信支持多图发送。
- 增加插件 WebUI，可扫码登录、查看运行状态，并浏览 SQLite 评论/私信统计、状态分布、筛选分页和消息详情。
- 增加插件页面中英文 i18n；AstrBot 最低版本调整为 `4.24.5`。

### 安全与稳定性

- 小黑盒用户 ID 使用 `xhh:` 命名空间，避免与 AstrBot 管理员身份混淆。
- 小黑盒标准事件默认禁止调用账号 LLM 工具，外部内容不能借提示注入读取私密数据或执行写操作。
- 本地工具图片仅对管理员开放，并限制在插件数据目录、可选系统临时目录和显式允许目录内。
- WebUI API 由 AstrBot Dashboard 登录态保护；可关闭消息正文显示，页面不会取得数据库路径、Cookie 或代理凭据。
- 增加私信数据库、图片上传、标准事件、工具隔离和 WebUI API 回归测试。

### 文档

- 重写世界书、私信、图片与 WebUI 说明，移除旧版本已经不再成立的限制。
- 更新与 `astrbot_plugin_xiaoheihe_adapter` 的能力对照，并增加第三方代码来源与许可证说明。

## v0.5.0 - 2026-07-26

### 新增

- 使用标准库 SQLite 自动归档收到的评论和 Bot 发出的评论，不需要额外数据库服务。
- 收到的评论按“帖子 ID + 评论 ID”去重，同时保留平台通知原始观察数，可区分重复推送与实际评论数。
- 增加私密 LLM 工具 `xhh_comment_stats` 和 `xhh_search_comment_archive`，支持按关键词、时间、帖子、用户、根楼、来源、状态和 Bot 评论类型统计或查询。
- 自动回复、自动巡帖评论和 `xhh_create_comment` 成功或发送结果不确定时会写入 Bot 评论归档。
- `/小黑盒状态` 显示评论原始观察数、去重数和 Bot 评论数。

### 文档

- 增加与 `advent259141/astrbot_plugin_xiaoheihe_adapter` 的能力对照，明确标准平台事件、私信自动对话、本地图片上传、WebUI 和 i18n 等架构差异。
- 补充两个参考项目的用途与代码边界；本插件不打包或运行参考项目代码。

## v0.4.2 - 2026-07-26

### 新增

- 写工具支持关闭逐次确认。关闭 `tools.require_explicit_confirmation` 并重载插件后，LLM 工具会移除 `confirm` 参数和确认提示，可按用户的明确自然语言要求直接执行。
- 自动回复和自动巡帖评论成功时记录 AstrBot 后台日志，包含相关 ID、对方评论或帖子标题，以及实际发送文本。

### 优化

- 自动回复成功通知现在同时显示对方评论、Bot 回复、消息 ID、帖子 ID、评论 ID、根评论 ID 和用户 ID。
- 自动巡帖评论成功通知现在显示帖子标题、Bot 评论、帖子 ID 和作者 ID。
- 移除 AstrBot 主动通知中的 `[小黑盒机器人]` 固定前缀。
- 将确认开关移动到写工具开关旁边，并补全开启、关闭后的行为说明。
- `/小黑盒帮助` 和 `/小黑盒状态` 会显示当前逐次确认模式。

## v0.4.1 - 2026-07-26

- 支持通过私有 SOCKS5 代理统一转发小黑盒请求，并使用代理端 DNS 解析。
- 代理不可用时停止请求，不回退云服务器直连；状态页与错误信息不会泄露代理凭据。
- 增加 Windows、OpenWrt/Linux 与 SSH 反向隧道家庭出口说明。

## v0.4.0 - 2026-07-26

- 增加自主巡帖：模型可选帖、阅读正文、决定跳过或发表评论。
- 增加频率、每日额度、作者冷却、内容过滤、去重和发送不确定保护。
- 支持自动回复 Bot 自己帖子下无需 `@` 的普通评论。

## v0.3.1 - 2026-07-26

- 整理 AstrBot 配置分组、字段说明和显眼提示。
- 插件显示名称调整为“小黑盒bot”，并优化 README 可读性。

## v0.3.0 - 2026-07-26

- 改为只运行 AstrBot 的独立插件，不再依赖额外的 xhhRobot/Go 服务。
- 提供扫码登录、人设自动回复和 20 个小黑盒 LLM 工具。
- 增加写入权限、确认词、限速、重复写入与发送不确定保护。
